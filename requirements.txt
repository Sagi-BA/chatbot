streamlit
streamlit-carousel
streamlit-extras #https://arnaudmiribel.github.io/streamlit-extras/
python-dotenv
aiohttp #This is for telegram
requests
groq
openai>=1.0.0
PyMuPDF
numpy
bcrypt>=4.0.1

# MPA/sambert embedding support (Hebrew-optimized)
sentence-transformers>=2.2.2
torch>=2.0.0
transformers>=4.35.0
huggingface-hub>=0.19.0


# Summary:
# git reset --hard: Discards any local changes.
# git fetch --all: Fetches the latest changes from the remote repository.
# git reset --hard origin/main: Resets your local branch to the latest commit from the remote branch.
# git clean -fd: (Optional) Cleans untracked files.

# 1. For VS Code:
# Open the Command Palette (Ctrl+Shift+P).
# Type Python: Select Interpreter.